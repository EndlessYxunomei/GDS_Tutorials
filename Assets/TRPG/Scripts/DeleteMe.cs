using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeleteMe : MonoBehaviour
{
    //For test only. Rewrite after
    
    // Start is called before the first frame update
    void Start()
    {
        Destroy(gameObject);
    }
}
